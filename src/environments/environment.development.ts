//environment.development =>case development(did not work building or production project)
export const environment = {
  // day 5 task2
production:false,
//URL You need to prove unchanged
APIURL : "http://localhost:3000"
};
